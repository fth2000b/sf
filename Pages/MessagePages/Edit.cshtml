@page
@model ShopFinder.Pages.MessagePages.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>RequestMessage</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="RequestMessage.ID" />
            <div class="form-group">
                <label asp-for="RequestMessage.RequestsID" class="control-label"></label>
                <input asp-for="RequestMessage.RequestsID" class="form-control" />
                <span asp-validation-for="RequestMessage.RequestsID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RequestMessage.Messsage" class="control-label"></label>
                <input asp-for="RequestMessage.Messsage" class="form-control" />
                <span asp-validation-for="RequestMessage.Messsage" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RequestMessage.MessageType" class="control-label"></label>
                <input asp-for="RequestMessage.MessageType" class="form-control" />
                <span asp-validation-for="RequestMessage.MessageType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RequestMessage.Updated" class="control-label"></label>
                <input asp-for="RequestMessage.Updated" class="form-control" />
                <span asp-validation-for="RequestMessage.Updated" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
